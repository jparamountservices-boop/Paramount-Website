services:
  web:
    build: .
    image: paramount-website
    restart: unless-stopped
    ports:
      - "8080:80"   # map to your reverse proxy / host port
    # For automatic HTTPS, front this with Caddy, Nginx Proxy Manager,
    # or Traefik. See docs/deployment/self-hosting.md.
